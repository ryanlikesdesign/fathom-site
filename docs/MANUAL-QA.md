# Manual QA — run before launch

In `npm run dev`:
- [ ] Tab through every page: visible focus on all links, buttons, and form fields.
- [ ] Theme toggle persists across reload and respects system preference on first visit.
- [ ] macOS System Settings → Accessibility → Display → Reduce Motion ON → confirm no transitions animate.
- [ ] VoiceOver (Safari): heading navigation works on every page; form validation errors are announced; the success message is announced after submitting.
- [ ] Submit the feedback form end-to-end against a real CONTACT_EMAIL and confirm the email arrives.
