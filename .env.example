RESEND_API_KEY=
CONTACT_EMAIL=support@fathomvision.app
FROM_EMAIL=Fathom <support@fathomvision.app>

# Optional: front-end password for the /promo rep page.
# Falls back to the default in lib/promo.ts if unset. Not real security —
# it only keeps the codes out of casual view.
NEXT_PUBLIC_PROMO_PASSWORD=
